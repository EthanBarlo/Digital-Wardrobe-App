<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MyDigitalWardrobe.ViewModels"
             x:DataType="viewmodels:ViewItemsViewModel"
             x:Class="MyDigitalWardrobe.Views.ViewItems">
    <ContentPage.BindingContext>
        <viewmodels:ViewItemsViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>

            <CollectionView x:Name="itemsList">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout BackgroundColor="Black">
                            <Label Text="{Binding Name}" FontSize="Medium"/>
                            <Label Text="{Binding Price}" FontSize="Medium"/>
                            <Label Text="{Binding LocationName}" FontSize="Medium"/>
                            <Label Text="{Binding DatePurchased}" FontSize="Medium"/>
                            <Label Text="{Binding WarrantyEnd}" FontSize="Medium"/>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>