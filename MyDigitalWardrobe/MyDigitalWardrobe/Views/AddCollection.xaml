<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MyDigitalWardrobe.ViewModels"
             x:DataType="viewmodels:AddCollectionViewModel"
             x:Class="MyDigitalWardrobe.Views.AddCollection">
    <ContentPage.BindingContext>
        <viewmodels:AddCollectionViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Name" FontSize="Medium"/>
            <Entry Text="{Binding Name, Mode=TwoWay}" 
                       FontSize="Medium"/>
            <Button Text="Create Collection" Command="{Binding CreateCommand}"/>
            <Button Text="Close" Command="{Binding CloseCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>